SCRIPT_NAME=$(basename -- "$0")
log() {
    printf "[INFO] ${SCRIPT_NAME}: %s\n" "$@"
}
loge() {
    printf "[ERR] ${SCRIPT_NAME}: %s\n" "$@" >&2
}
logd() {
    if [ ${DEBUG:-} ]; then
        printf "[DEBUG] ${SCRIPT_NAME}: %s\n" "$@"
    fi
}